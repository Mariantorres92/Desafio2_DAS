@model IEnumerable<ColegioSanJose.Models.Expediente>

@{
    ViewData["Title"] = "Expedientes";
}

<h2>Lista de Expedientes</h2>

<p>
    <a asp-action="Create" class="btn btn-lg" style="background-color:#16A34A; color:white;">Agregar Nuevo Expediente</a>
</p>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Alumno</th>
            <th>Materia</th>
            <th>Nota Final</th>
            <th>Observaciones</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Alumno?.Nombre @item.Alumno?.Apellido</td>
                <td>@item.Materia?.NombreMateria</td>
                <td>@item.NotaFinal</td>
                <td>@item.Observaciones</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ExpedienteId" class="btn btn-sm" style="background-color:#16A34A; color:white;">Editar</a>
                    <a asp-action="Details" asp-route-id="@item.ExpedienteId" class="btn btn-sm" style="background-color:#6B7280; color:white;">Detalles</a>
                    <a asp-action="Delete" asp-route-id="@item.ExpedienteId" class="btn btn-sm btn-danger">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>